image: node:10

stages:
  - test

run_tests:
  stage: test
  script:
    - node --version
    - npm --version
    - env
    #- npm install github:nearprotocol/nearlib#nightshade
    - npm install 
    - ls -lh node_modules/nearlib/lib
    - ./bin/near state test.near
    #- npm install
    #- npm run test
